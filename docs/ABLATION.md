# 有限消融验证

执行日期：2026-09-08。使用现有 Vitest，在 `.cache/ablation/run-.../` 的隔离副本中逐项移除保护逻辑；不修改产品源文件，不加入生产绕过开关。

```powershell
npm run test:ablation
```

| 变体 | 测试退出码 | 结果 |
| --- | ---: | --- |
| 原始基线 | 0 | 26/26通过：23项服务端、3项播放流程 |
| 移除 requiredClaims.exp | 1 | 恰好1个预期失败：错误接受无exp的有效签名令牌 |
| 移除 jwtVerify.audience | 1 | 恰好1个预期失败：错误接受其他受众的令牌 |
| 移除旧播放请求结果保护 | 1 | 恰好1个预期失败：旧A票据覆盖最新B的音频地址 |

三种消融均产生对应断言失败，无导入或语法错误；原始基线保持通过。这说明这些保护对已覆盖的风险有实际作用，不是仅靠构建成功判断正确性。

[脚本](../scripts/ablation.mjs) 先运行基线，再执行三个独立变体。每个精确替换只能匹配一次；使用JSON报告判断失败名称与数量，额外失败、没有测试、导入/语法错误都不会算作有效消融。各次执行创建新目录，node_modules复用项目依赖。

机器结果在 [results.json](../.cache/ablation/results.json)，含退出码、断言、报告路径和分类；缓存目录不提交仓库，可按上述命令重新生成。整个脚本退出0表示基线正常且三个消融都按预期暴露问题。

边界：JWT使用本地密钥和模拟JWKS，播放流程使用FakeAudio。消融不访问Cloudflare/B2，也不替代真实播放、网络Range、生产鉴权或实际签名过期验收。为控制范围，不继续扩大到每个UI状态或库内部实现。

