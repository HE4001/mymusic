# 播放竞争消融

保留一个实际播放风险的消融，不增加生产绕过开关。早期 Access/JWT 消融已移除；网站目前使用共享密码会话，消融对象仅为播放请求竞争保护。

运行 `npm run test:ablation`。脚本在新的 `.cache/ablation` 隔离目录复制所需源文件，先验证播放与服务端基线，再精确移除旧票据结果保护。此脚本只运行选定测试，不等同于完整测试套件。

| 变体 | 结果 |
| --- | --- |
| baseline | 28/28通过（2026-09-16） |
| remove-stale-ticket-guard | 恰好1个预期断言失败：旧A请求的票据覆盖最新B音频地址 |

本轮实际执行通过：无导入/语法错误、无额外失败。脚本退出0表示基线及消融均符合预期。机器报告写入[results.json](../.cache/ablation/results.json)，不提交缓存。

这项测试使用FakeAudio和模拟票据，不代表真实B2联调；实际源文件不变。
