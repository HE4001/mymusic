# 中断续作与有限消融（2026-09-08）

上一轮 sol 代理因账号用量限制终止，旧句柄现已 not_found。以磁盘现状续作，不重新搭项目。用户仍只要求可部署代码，真实云配置之后再做。

## A2 前端收尾 — sol xhigh

沿用01界面任务和src/types.ts，仅修改 src/App.tsx、src/styles.css、src/components/**，以及误落在 C/Users/David/Documents/ChatGPT/远程播放器/src/components/TrackList.tsx 的文件。先将该文件内容放回正确目录，再删除误落的单个文件，别递归删除未知目录。修复中断产生的 JSX 错字/缺模块；usePlayer 必须传 libraryLoaded: libraryState==='ready'。认证提示只识别401/AUTH_REQUIRED，不能把ACCESS_CONFIG_ERROR等500当成登录失效。保留设计中的实际功能，别加新页面。代码级审查进度拖动、手机dialog焦点和键盘操作。提交完整模块及有限检查结果；不改hook或package。主代理负责统一构建。

## B2 播放控制收尾 — sol xhigh

沿用02任务和共享类型，只修改 src/hooks/**、src/lib/**、tests/player*.test.ts、tests/queue*.test.ts、tests/preferences*.test.ts。当前usePlayer中断在requestTicket并返回空类型断言，必须完成真实controller；可简化现有重复state/ref辅助代码，不扩展功能。已有queue/preferences/api/demo helper能复用。必须保护快速切歌、暂停意图、单次过期恢复、metadata取消、StrictMode单实例清理、libraryLoaded与本机偏好。演示模式不读写生产偏好。测试用happy-dom（主代理安装），React act+createRoot渲染小hook harness和FakeAudio即可，不装testing-library。仅挑3～5个实际流程回归：乱序票据/暂停等待/空库加载/过期恢复等；不要测试一个布尔辅助函数冒充整条异步流程。给出一个可单独移除的关键防护及对应会失败用例，供主代理做播放消融。公开类型不能改，不写UI或后端。

## E 后端消融脚本 — luna max

独占 scripts/ablation.mjs、docs/ABLATION.md；只读server/access.ts和tests/server.test.ts。创建一个简单Node脚本，用fs.cp或逐个复制将server/functions/src/types.ts/tests/server.test.ts/vitest.config.ts复制到项目.cache/ablation内隔离目录，使用原项目node_modules内Vitest执行。先baseline通过，再分别移除JWT requiredClaims中的exp、移除jwtVerify audience配置。每次只改隔离副本；不得修改真实源文件；不得添加生产开关。每个突变必须精确匹配一次，否则脚本报错。记录各次退出码及失败用例到.cache/ablation/results.json，并在docs/ABLATION.md提供方法和局限。mutant非零不一定有效：须区分导入/语法错误与预期断言失败，可使用Vitest JSON报告断言相应测试失败。2个有效突变足够；不遍历全项目、不联网、不安装依赖、不构建通用mutation框架。目录安全：只清理脚本自己创建且已解析确认在.cache/ablation内的副本，或者采用每次新目录避免删除。主代理负责执行脚本和更新结果。

主代理继续审查后端、完整类型/构建、模块接线与产物；不会把尚未实现的hook或类型断言当作完成。

## F 播放流程验证 — sol xhigh

为避免播放实现与测试串行等待，测试写入范围现从B2移交F：仅 tests/player.test.ts。B2只完成src/hooks/**与src/lib/**。测试不读私有ref，不改生产代码，不新增依赖。

使用已安装happy-dom、React act/createRoot和FakeAudio(EventTarget)。以公开usePlayer契约驱动三个完整流程：①先选A后选B、票据B先回A后回，src与当前曲目仍为B；②取票据/metadata等待期间pause，完成加载后不调用play、保持暂停；③首次空数组且libraryLoaded=false保留已存收藏与位置，加载真实曲库后恢复为暂停、无自动请求，成功空库才清理。用vi.mock api的可控Promise，不用计时sleep。每个测试清理root/全局mock，不要求真正解码音频。报告一个适合消融的明确实现guard（若还未落盘可待主代理指定）。只写这3个风险测试，主代理运行并与B2接线，不扩展框架或UI按钮测试。
